package bac.week4

/*
    동전 뒤집기.
 */

//lateinit var arr: Array<CharArray>
//
//fun bac.week5.concept.main() = with(System.`in`.bufferedReader()) {
//
//    val bac.week5.concept.getN = readLine().toInt()
//
//    arr = Array(bac.week5.concept.getN) { CharArray(bac.week5.concept.getN) { ' ' } }
//    for (i in 0 until bac.week5.concept.getN) {
//        arr[i] = readLine().toCharArray()
//    }
////    arr.forEach { println(it.joinToString(" ")) }
//}